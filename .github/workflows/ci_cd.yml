name: CI/CD Pipeline

on:
    workflow_dispatch:
    
permissions:
  contents: read
  id-token: write
  
jobs:
    ci:
        uses: ./.github/workflows/ci.yml
        secrets: inherit

    cd:
        needs: ci
        uses: ./.github/workflows/cd.yml
        secrets: inherit
